# ─────────────────────────────────────────────────────────────
# TEMPLATE: Orchestrator Agent
# Role      : Full system control, orchestration level.
# Auth Level: 3 — highest privilege.
# Use case  : System-wide changes, coordinating other agents.
# ─────────────────────────────────────────────────────────────
agent:
  id: "orchestrator-01"
  role: "Orchestrator"
  skill_set:
    - "code-analysis"
    - "security-audit"
    - "refactor"
  authorization_level: 3
  read_only: false
